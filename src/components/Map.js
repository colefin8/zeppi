import React from "react";
import LeafletMap from './LeafletMap';

const MainMap = (props) => {

  return (
      <div className="MainMap">
        <LeafletMap/>
      </div>
  );
}

export default MainMap;